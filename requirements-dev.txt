# Зависимости для разработки и тестов.
# Тесты написаны на unittest (stdlib) и запускаются без доп. установок:
#   python3 -m unittest discover -s tests
# При желании можно гонять их через pytest (запускает unittest-классы тоже).
pytest>=7.0
